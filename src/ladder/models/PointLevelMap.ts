export default class PointsLevelMap {
	public readonly points: number;
	public readonly level: string;

	constructor(points: number, level: string) {
		this.points = points;
		this.level = level;
	}
}
